/**
 * import something from mui that is using `classnames`
 */
import React from "react";
import ReactDOM from "react-dom";

import { Typography } from "@material-ui/core";

ReactDOM.render(<Typography />, document.getElementById("root"));
